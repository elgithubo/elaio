- add marker for query channel of transformer (position encoding)
- make persistence support attention & consider shared parameters
- add potential to calculator
- evaluate if a gradient check (cosine?) makes sense (maybe not required if bias is in place)
- check save directory exists before training
- add check that loading a persistet state is for the correct network operation, e.g. addition
- add check during loading a persisted state that NeuronType fits
- add a test suite for 'sbt test'
