package elaio.neuralnet.test

// Runs the Calculator task with global two-pass attention enabled.
final class AttentionTest extends CalculatorTest {
  override protected val attentionEnabled = true
}
